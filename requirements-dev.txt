-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
httpx>=0.27                # async test client for FastAPI

# ── Training pipeline ─────────────────────────────────────────────────────────
xgboost>=1.7.0             # model training (not needed at serve time — API uses ONNX)
mlflow>=2.0.0              # experiment tracking + model registry
optuna>=3.0.0              # hyperparameter search
pandera>=0.20.0            # data quality validation in retrain pipeline

# ── ONNX export ───────────────────────────────────────────────────────────────
onnxmltools>=1.12.0        # convert XGBoost → ONNX
skl2onnx>=0.5.0            # convert scikit-learn → ONNX
